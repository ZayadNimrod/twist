use twist;
use twist::sim::game::Game;

fn main() {
    //Set up game
    let state: Game = twist::sim::game::set_up_game();

    //TODO:while turn is not over, get the phasing players action, check that it is valid, and resolve it.
}
